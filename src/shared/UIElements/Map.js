import React from "react";

const Map = props => {
    return <div className={`map ${props.className}`}>
        <h2>Here it will go the map, but I need a Credit Card for that so...!</h2>
    </div>
};

export default Map;